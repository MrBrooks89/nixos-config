# ignore everything
/*
# do not ignore the .gitignore file
!.gitignore
!/.dotfiles
!.zshrc
# do not ignore .config
!.config
# ignore  files and folders in the .config directory
/.config/*
# do not igonre these files and folders in .config directory
!/.config/hypr
!/.config/starship.toml
!/.config/kitty
!/.config/waybar
